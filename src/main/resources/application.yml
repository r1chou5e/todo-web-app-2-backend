spring:
  application:
    name: todo-app-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/todo-app
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
